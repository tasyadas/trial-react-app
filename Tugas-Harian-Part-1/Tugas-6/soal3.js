let limit = 20
let count = 0

while (limit > count) {
    count += 2
    console.log(`${count} - I love coding`);
}

console.log('\n');

while (count >= 2) {
    console.log(`${count} - I will become a frontend developer`);
    count -= 2
}
